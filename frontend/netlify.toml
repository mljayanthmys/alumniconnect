# frontend/netlify.toml

[build]
  base    = "."
  command = "npm run build"
  publish = "dist/frontend"  # <-- CRITICAL FIX: The correct output folder

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"